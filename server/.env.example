# Copy to .env (untracked) and fill in real values before `docker compose up`.
CELLCOUNTS_USER=changeme
CELLCOUNTS_PASS=changeme

# Uncomment to force CPU (debugging only — production should have a GPU):
# CELLCOUNTS_CPU=1
